export interface BookGenre {
  id: number; // Unique identifier for the genre
  genreType: string; // The type of the book genre
  createdAt: string; // ISO 8601 string
  updatedAt: string; // ISO 8601 string
}